# إعدادات الإنتاج للـ backend
NODE_ENV=production
PORT=3000

# بيانات قاعدة البيانات (يمكن تعديل كلمة المرور لاحقاً)
DATABASE_URL=postgresql://postgres:StrongDBPasswordHere@db:5432/elmostashar

# مفتاح JWT سري (اكتب قيمة عشوائية قوية)
JWT_SECRET=ChangeThisToAReallyStrongSecretKey123

# مفتاح OpenAI (اختياري إن كنت تستخدمه)
OPENAI_API_KEY=your_openai_api_key_here

# إعدادات CORS (النطاق الذي سيسمح له بالاتصال بالواجهة الخلفية)
CORS_ORIGIN=http://localhost
